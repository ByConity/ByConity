#pragma once
#include <Statistics/StatisticsBaseImpl.h>

#if 0
namespace DB::Statistics
{
// TODO(gouguilin): use protobuf enum instead of SerdeDataType to improve compatibility
void patchDbStats(Protos::DbStats & db_stats);
}
#endif
