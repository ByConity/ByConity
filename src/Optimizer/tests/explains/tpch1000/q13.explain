Projection Est. 102503752 rows
│     Expressions: c_count:=`expr#count(o_orderkey)`, custdist:=`expr#count()`
└─ Sorting Est. 102503752 rows
   │     Order by: {expr#count() desc nulls_last, expr#count(o_orderkey) desc nulls_last}
   └─ Gather Exchange Est. 102503752 rows
      └─ Sorting Est. 102503752 rows
         │     Order by: {expr#count() desc nulls_last, expr#count(o_orderkey) desc nulls_last}
         └─ MergingAggregated Est. 102503752 rows
            └─ Repartition Exchange Est. 102503752 rows
               │     Partition by: {expr#count(o_orderkey)}
               └─ Aggregating Est. 102503752 rows
                  │     Group by: {expr#count(o_orderkey)}
                  │     Aggregates: expr#count():=AggNull(count)()
                  └─ Projection Est. 102503752 rows
                     │     Expressions: [expr#count(o_orderkey)]
                     └─ Aggregating Est. 102503752 rows
                        │     Group by: {c_custkey}
                        │     Aggregates: expr#count(o_orderkey):=AggNull(count)(o_orderkey)
                        └─ Left (PARALLEL_HASH) Join Est. 1500000000 rows
                           │     Condition: c_custkey == o_custkey
                           │     Filter: o_comment NOT LIKE '%special%requests%'
                           ├─ Repartition Exchange Est. 150000000 rows
                           │  │     Partition by: {c_custkey}
                           │  └─ TableScan tpch1000.customer Est. 150000000 rows
                           │           Outputs: [c_custkey]
                           └─ Repartition Exchange Est. 1500000000 rows
                              │     Partition by: {o_custkey}
                              └─ TableScan tpch1000.orders Est. 1500000000 rows
                                       Outputs: [o_comment, o_custkey, o_orderkey]
