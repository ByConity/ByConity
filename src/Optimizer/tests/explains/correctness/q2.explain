Projection Est. ? rows
│     Expressions: [a]
└─ Sorting Est. ? rows
   │     Order by: {a asc, b asc}
   └─ Gather Exchange Est. ? rows
      └─ Sorting Est. ? rows
         │     Order by: {a asc, b asc}
         └─ TableScan correctness.q2_t1 Est. ? rows
                  Outputs: [a, b]
