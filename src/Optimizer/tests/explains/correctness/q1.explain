Projection est. 2 rows
└─ Union est. 2 rows
   ├─ Gather Exchange est. ? rows
   │  └─ TableScan correctness.q1_t1 est. ? rows
   └─ Limit est. 2 rows
      └─ Sorting est. ? rows
         │     Order by: {a_1 asc}
         └─ Gather Exchange est. ? rows
            └─ Sorting est. ? rows
               │     Order by: {a_1 asc}
               └─ TableScan correctness.q1_t1 est. ? rows
