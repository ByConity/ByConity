Projection est. ? rows
└─ Inner Join est. ? rows
   │     Condition: 
   ├─ Inner Join est. ? rows
   │  │     Condition: 
   │  ├─ Inner Join est. ? rows
   │  │  │     Condition: 
   │  │  ├─ Inner Join est. ? rows
   │  │  │  │     Condition: 
   │  │  │  ├─ Inner Join est. ? rows
   │  │  │  │  │     Condition: 
   │  │  │  │  ├─ Inner Join est. ? rows
   │  │  │  │  │  │     Condition: 
   │  │  │  │  │  ├─ Inner Join est. ? rows
   │  │  │  │  │  │  │     Condition: 
   │  │  │  │  │  │  ├─ MergingAggregated est. ? rows
   │  │  │  │  │  │  │  └─ Gather Exchange est. ? rows
   │  │  │  │  │  │  │     └─ Aggregating est. ? rows
   │  │  │  │  │  │  │        │     Group by: {}
   │  │  │  │  │  │  │        └─ Inner Join est. ? rows
   │  │  │  │  │  │  │           │     Condition: ss_store_sk == s_store_sk
   │  │  │  │  │  │  │           ├─ Repartition Exchange est. ? rows
   │  │  │  │  │  │  │           │  │     Partition by: {ss_store_sk}
   │  │  │  │  │  │  │           │  └─ Inner Join est. ? rows
   │  │  │  │  │  │  │           │     │     Condition: ss_sold_time_sk == t_time_sk
   │  │  │  │  │  │  │           │     ├─ Repartition Exchange est. ? rows
   │  │  │  │  │  │  │           │     │  │     Partition by: {ss_sold_time_sk}
   │  │  │  │  │  │  │           │     │  └─ Inner Join est. ? rows
   │  │  │  │  │  │  │           │     │     │     Condition: ss_hdemo_sk == hd_demo_sk
   │  │  │  │  │  │  │           │     │     ├─ Repartition Exchange est. ? rows
   │  │  │  │  │  │  │           │     │     │  │     Partition by: {ss_hdemo_sk}
   │  │  │  │  │  │  │           │     │     │  └─ TableScan tpcds.store_sales est. ? rows
   │  │  │  │  │  │  │           │     │     └─ Repartition Exchange est. ? rows
   │  │  │  │  │  │  │           │     │        │     Partition by: {hd_demo_sk}
   │  │  │  │  │  │  │           │     │        └─ Filter est. ? rows
   │  │  │  │  │  │  │           │     │           │     ((hd_dep_count = 0) AND (hd_vehicle_count <= cast(2, 'UInt16'))) OR ((hd_dep_count = 2) AND (hd_vehicle_count <= cast(4, 'UInt16'))) OR ((hd_dep_count = 4) AND (hd_vehicle_count <= cast(6, 'UInt16')))
   │  │  │  │  │  │  │           │     │           └─ TableScan tpcds.household_demographics est. ? rows
   │  │  │  │  │  │  │           │     └─ Repartition Exchange est. ? rows
   │  │  │  │  │  │  │           │        │     Partition by: {t_time_sk}
   │  │  │  │  │  │  │           │        └─ Filter est. ? rows
   │  │  │  │  │  │  │           │           │     (t_hour = 8) AND (t_minute >= 30)
   │  │  │  │  │  │  │           │           └─ TableScan tpcds.time_dim est. ? rows
   │  │  │  │  │  │  │           └─ Repartition Exchange est. ? rows
   │  │  │  │  │  │  │              │     Partition by: {s_store_sk}
   │  │  │  │  │  │  │              └─ Filter est. ? rows
   │  │  │  │  │  │  │                 │     s_store_name = 'ese'
   │  │  │  │  │  │  │                 └─ TableScan tpcds.store est. ? rows
   │  │  │  │  │  │  └─ Broadcast Exchange est. ? rows
   │  │  │  │  │  │     └─ MergingAggregated est. ? rows
   │  │  │  │  │  │        └─ Gather Exchange est. ? rows
   │  │  │  │  │  │           └─ Aggregating est. ? rows
   │  │  │  │  │  │              │     Group by: {}
   │  │  │  │  │  │              └─ Inner Join est. ? rows
   │  │  │  │  │  │                 │     Condition: ss_store_sk_1 == s_store_sk_1
   │  │  │  │  │  │                 ├─ Repartition Exchange est. ? rows
   │  │  │  │  │  │                 │  │     Partition by: {ss_store_sk_1}
   │  │  │  │  │  │                 │  └─ Inner Join est. ? rows
   │  │  │  │  │  │                 │     │     Condition: ss_sold_time_sk_1 == t_time_sk_1
   │  │  │  │  │  │                 │     ├─ Repartition Exchange est. ? rows
   │  │  │  │  │  │                 │     │  │     Partition by: {ss_sold_time_sk_1}
   │  │  │  │  │  │                 │     │  └─ Inner Join est. ? rows
   │  │  │  │  │  │                 │     │     │     Condition: ss_hdemo_sk_1 == hd_demo_sk_1
   │  │  │  │  │  │                 │     │     ├─ Repartition Exchange est. ? rows
   │  │  │  │  │  │                 │     │     │  │     Partition by: {ss_hdemo_sk_1}
   │  │  │  │  │  │                 │     │     │  └─ TableScan tpcds.store_sales est. ? rows
   │  │  │  │  │  │                 │     │     └─ Repartition Exchange est. ? rows
   │  │  │  │  │  │                 │     │        │     Partition by: {hd_demo_sk_1}
   │  │  │  │  │  │                 │     │        └─ Filter est. ? rows
   │  │  │  │  │  │                 │     │           │     ((hd_dep_count_1 = 4) AND (hd_vehicle_count_1 <= cast(6, 'UInt16'))) OR ((hd_vehicle_count_1 <= cast(4, 'UInt16')) AND (hd_dep_count_1 = 2)) OR ((hd_vehicle_count_1 <= cast(2, 'UInt16')) AND (hd_dep_count_1 = 0))
   │  │  │  │  │  │                 │     │           └─ TableScan tpcds.household_demographics est. ? rows
   │  │  │  │  │  │                 │     └─ Repartition Exchange est. ? rows
   │  │  │  │  │  │                 │        │     Partition by: {t_time_sk_1}
   │  │  │  │  │  │                 │        └─ Filter est. ? rows
   │  │  │  │  │  │                 │           │     (t_minute_1 < 30) AND (t_hour_1 = 9)
   │  │  │  │  │  │                 │           └─ TableScan tpcds.time_dim est. ? rows
   │  │  │  │  │  │                 └─ Repartition Exchange est. ? rows
   │  │  │  │  │  │                    │     Partition by: {s_store_sk_1}
   │  │  │  │  │  │                    └─ Filter est. ? rows
   │  │  │  │  │  │                       │     s_store_name_1 = 'ese'
   │  │  │  │  │  │                       └─ TableScan tpcds.store est. ? rows
   │  │  │  │  │  └─ Broadcast Exchange est. ? rows
   │  │  │  │  │     └─ MergingAggregated est. ? rows
   │  │  │  │  │        └─ Gather Exchange est. ? rows
   │  │  │  │  │           └─ Aggregating est. ? rows
   │  │  │  │  │              │     Group by: {}
   │  │  │  │  │              └─ Inner Join est. ? rows
   │  │  │  │  │                 │     Condition: ss_store_sk_2 == s_store_sk_2
   │  │  │  │  │                 ├─ Repartition Exchange est. ? rows
   │  │  │  │  │                 │  │     Partition by: {ss_store_sk_2}
   │  │  │  │  │                 │  └─ Inner Join est. ? rows
   │  │  │  │  │                 │     │     Condition: ss_sold_time_sk_2 == t_time_sk_2
   │  │  │  │  │                 │     ├─ Repartition Exchange est. ? rows
   │  │  │  │  │                 │     │  │     Partition by: {ss_sold_time_sk_2}
   │  │  │  │  │                 │     │  └─ Inner Join est. ? rows
   │  │  │  │  │                 │     │     │     Condition: ss_hdemo_sk_2 == hd_demo_sk_2
   │  │  │  │  │                 │     │     ├─ Repartition Exchange est. ? rows
   │  │  │  │  │                 │     │     │  │     Partition by: {ss_hdemo_sk_2}
   │  │  │  │  │                 │     │     │  └─ TableScan tpcds.store_sales est. ? rows
   │  │  │  │  │                 │     │     └─ Repartition Exchange est. ? rows
   │  │  │  │  │                 │     │        │     Partition by: {hd_demo_sk_2}
   │  │  │  │  │                 │     │        └─ Filter est. ? rows
   │  │  │  │  │                 │     │           │     ((hd_vehicle_count_2 <= cast(6, 'UInt16')) AND (hd_dep_count_2 = 4)) OR ((hd_dep_count_2 = 0) AND (hd_vehicle_count_2 <= cast(2, 'UInt16'))) OR ((hd_dep_count_2 = 2) AND (hd_vehicle_count_2 <= cast(4, 'UInt16')))
   │  │  │  │  │                 │     │           └─ TableScan tpcds.household_demographics est. ? rows
   │  │  │  │  │                 │     └─ Repartition Exchange est. ? rows
   │  │  │  │  │                 │        │     Partition by: {t_time_sk_2}
   │  │  │  │  │                 │        └─ Filter est. ? rows
   │  │  │  │  │                 │           │     (t_minute_2 >= 30) AND (t_hour_2 = 9)
   │  │  │  │  │                 │           └─ TableScan tpcds.time_dim est. ? rows
   │  │  │  │  │                 └─ Repartition Exchange est. ? rows
   │  │  │  │  │                    │     Partition by: {s_store_sk_2}
   │  │  │  │  │                    └─ Filter est. ? rows
   │  │  │  │  │                       │     s_store_name_2 = 'ese'
   │  │  │  │  │                       └─ TableScan tpcds.store est. ? rows
   │  │  │  │  └─ Broadcast Exchange est. ? rows
   │  │  │  │     └─ MergingAggregated est. ? rows
   │  │  │  │        └─ Gather Exchange est. ? rows
   │  │  │  │           └─ Aggregating est. ? rows
   │  │  │  │              │     Group by: {}
   │  │  │  │              └─ Inner Join est. ? rows
   │  │  │  │                 │     Condition: ss_store_sk_3 == s_store_sk_3
   │  │  │  │                 ├─ Repartition Exchange est. ? rows
   │  │  │  │                 │  │     Partition by: {ss_store_sk_3}
   │  │  │  │                 │  └─ Inner Join est. ? rows
   │  │  │  │                 │     │     Condition: ss_sold_time_sk_3 == t_time_sk_3
   │  │  │  │                 │     ├─ Repartition Exchange est. ? rows
   │  │  │  │                 │     │  │     Partition by: {ss_sold_time_sk_3}
   │  │  │  │                 │     │  └─ Inner Join est. ? rows
   │  │  │  │                 │     │     │     Condition: ss_hdemo_sk_3 == hd_demo_sk_3
   │  │  │  │                 │     │     ├─ Repartition Exchange est. ? rows
   │  │  │  │                 │     │     │  │     Partition by: {ss_hdemo_sk_3}
   │  │  │  │                 │     │     │  └─ TableScan tpcds.store_sales est. ? rows
   │  │  │  │                 │     │     └─ Repartition Exchange est. ? rows
   │  │  │  │                 │     │        │     Partition by: {hd_demo_sk_3}
   │  │  │  │                 │     │        └─ Filter est. ? rows
   │  │  │  │                 │     │           │     ((hd_vehicle_count_3 <= cast(2, 'UInt16')) AND (hd_dep_count_3 = 0)) OR ((hd_vehicle_count_3 <= cast(6, 'UInt16')) AND (hd_dep_count_3 = 4)) OR ((hd_dep_count_3 = 2) AND (hd_vehicle_count_3 <= cast(4, 'UInt16')))
   │  │  │  │                 │     │           └─ TableScan tpcds.household_demographics est. ? rows
   │  │  │  │                 │     └─ Repartition Exchange est. ? rows
   │  │  │  │                 │        │     Partition by: {t_time_sk_3}
   │  │  │  │                 │        └─ Filter est. ? rows
   │  │  │  │                 │           │     (t_hour_3 = 10) AND (t_minute_3 < 30)
   │  │  │  │                 │           └─ TableScan tpcds.time_dim est. ? rows
   │  │  │  │                 └─ Repartition Exchange est. ? rows
   │  │  │  │                    │     Partition by: {s_store_sk_3}
   │  │  │  │                    └─ Filter est. ? rows
   │  │  │  │                       │     s_store_name_3 = 'ese'
   │  │  │  │                       └─ TableScan tpcds.store est. ? rows
   │  │  │  └─ Broadcast Exchange est. ? rows
   │  │  │     └─ MergingAggregated est. ? rows
   │  │  │        └─ Gather Exchange est. ? rows
   │  │  │           └─ Aggregating est. ? rows
   │  │  │              │     Group by: {}
   │  │  │              └─ Inner Join est. ? rows
   │  │  │                 │     Condition: ss_store_sk_4 == s_store_sk_4
   │  │  │                 ├─ Repartition Exchange est. ? rows
   │  │  │                 │  │     Partition by: {ss_store_sk_4}
   │  │  │                 │  └─ Inner Join est. ? rows
   │  │  │                 │     │     Condition: ss_sold_time_sk_4 == t_time_sk_4
   │  │  │                 │     ├─ Repartition Exchange est. ? rows
   │  │  │                 │     │  │     Partition by: {ss_sold_time_sk_4}
   │  │  │                 │     │  └─ Inner Join est. ? rows
   │  │  │                 │     │     │     Condition: ss_hdemo_sk_4 == hd_demo_sk_4
   │  │  │                 │     │     ├─ Repartition Exchange est. ? rows
   │  │  │                 │     │     │  │     Partition by: {ss_hdemo_sk_4}
   │  │  │                 │     │     │  └─ TableScan tpcds.store_sales est. ? rows
   │  │  │                 │     │     └─ Repartition Exchange est. ? rows
   │  │  │                 │     │        │     Partition by: {hd_demo_sk_4}
   │  │  │                 │     │        └─ Filter est. ? rows
   │  │  │                 │     │           │     ((hd_dep_count_4 = 0) AND (hd_vehicle_count_4 <= cast(2, 'UInt16'))) OR ((hd_vehicle_count_4 <= cast(6, 'UInt16')) AND (hd_dep_count_4 = 4)) OR ((hd_dep_count_4 = 2) AND (hd_vehicle_count_4 <= cast(4, 'UInt16')))
   │  │  │                 │     │           └─ TableScan tpcds.household_demographics est. ? rows
   │  │  │                 │     └─ Repartition Exchange est. ? rows
   │  │  │                 │        │     Partition by: {t_time_sk_4}
   │  │  │                 │        └─ Filter est. ? rows
   │  │  │                 │           │     (t_hour_4 = 10) AND (t_minute_4 >= 30)
   │  │  │                 │           └─ TableScan tpcds.time_dim est. ? rows
   │  │  │                 └─ Repartition Exchange est. ? rows
   │  │  │                    │     Partition by: {s_store_sk_4}
   │  │  │                    └─ Filter est. ? rows
   │  │  │                       │     s_store_name_4 = 'ese'
   │  │  │                       └─ TableScan tpcds.store est. ? rows
   │  │  └─ Broadcast Exchange est. ? rows
   │  │     └─ MergingAggregated est. ? rows
   │  │        └─ Gather Exchange est. ? rows
   │  │           └─ Aggregating est. ? rows
   │  │              │     Group by: {}
   │  │              └─ Inner Join est. ? rows
   │  │                 │     Condition: ss_store_sk_5 == s_store_sk_5
   │  │                 ├─ Repartition Exchange est. ? rows
   │  │                 │  │     Partition by: {ss_store_sk_5}
   │  │                 │  └─ Inner Join est. ? rows
   │  │                 │     │     Condition: ss_sold_time_sk_5 == t_time_sk_5
   │  │                 │     ├─ Repartition Exchange est. ? rows
   │  │                 │     │  │     Partition by: {ss_sold_time_sk_5}
   │  │                 │     │  └─ Inner Join est. ? rows
   │  │                 │     │     │     Condition: ss_hdemo_sk_5 == hd_demo_sk_5
   │  │                 │     │     ├─ Repartition Exchange est. ? rows
   │  │                 │     │     │  │     Partition by: {ss_hdemo_sk_5}
   │  │                 │     │     │  └─ TableScan tpcds.store_sales est. ? rows
   │  │                 │     │     └─ Repartition Exchange est. ? rows
   │  │                 │     │        │     Partition by: {hd_demo_sk_5}
   │  │                 │     │        └─ Filter est. ? rows
   │  │                 │     │           │     ((hd_vehicle_count_5 <= cast(6, 'UInt16')) AND (hd_dep_count_5 = 4)) OR ((hd_dep_count_5 = 0) AND (hd_vehicle_count_5 <= cast(2, 'UInt16'))) OR ((hd_dep_count_5 = 2) AND (hd_vehicle_count_5 <= cast(4, 'UInt16')))
   │  │                 │     │           └─ TableScan tpcds.household_demographics est. ? rows
   │  │                 │     └─ Repartition Exchange est. ? rows
   │  │                 │        │     Partition by: {t_time_sk_5}
   │  │                 │        └─ Filter est. ? rows
   │  │                 │           │     (t_hour_5 = 11) AND (t_minute_5 < 30)
   │  │                 │           └─ TableScan tpcds.time_dim est. ? rows
   │  │                 └─ Repartition Exchange est. ? rows
   │  │                    │     Partition by: {s_store_sk_5}
   │  │                    └─ Filter est. ? rows
   │  │                       │     s_store_name_5 = 'ese'
   │  │                       └─ TableScan tpcds.store est. ? rows
   │  └─ Broadcast Exchange est. ? rows
   │     └─ MergingAggregated est. ? rows
   │        └─ Gather Exchange est. ? rows
   │           └─ Aggregating est. ? rows
   │              │     Group by: {}
   │              └─ Inner Join est. ? rows
   │                 │     Condition: ss_store_sk_6 == s_store_sk_6
   │                 ├─ Repartition Exchange est. ? rows
   │                 │  │     Partition by: {ss_store_sk_6}
   │                 │  └─ Inner Join est. ? rows
   │                 │     │     Condition: ss_sold_time_sk_6 == t_time_sk_6
   │                 │     ├─ Repartition Exchange est. ? rows
   │                 │     │  │     Partition by: {ss_sold_time_sk_6}
   │                 │     │  └─ Inner Join est. ? rows
   │                 │     │     │     Condition: ss_hdemo_sk_6 == hd_demo_sk_6
   │                 │     │     ├─ Repartition Exchange est. ? rows
   │                 │     │     │  │     Partition by: {ss_hdemo_sk_6}
   │                 │     │     │  └─ TableScan tpcds.store_sales est. ? rows
   │                 │     │     └─ Repartition Exchange est. ? rows
   │                 │     │        │     Partition by: {hd_demo_sk_6}
   │                 │     │        └─ Filter est. ? rows
   │                 │     │           │     ((hd_vehicle_count_6 <= cast(4, 'UInt16')) AND (hd_dep_count_6 = 2)) OR ((hd_dep_count_6 = 4) AND (hd_vehicle_count_6 <= cast(6, 'UInt16'))) OR ((hd_dep_count_6 = 0) AND (hd_vehicle_count_6 <= cast(2, 'UInt16')))
   │                 │     │           └─ TableScan tpcds.household_demographics est. ? rows
   │                 │     └─ Repartition Exchange est. ? rows
   │                 │        │     Partition by: {t_time_sk_6}
   │                 │        └─ Filter est. ? rows
   │                 │           │     (t_hour_6 = 11) AND (t_minute_6 >= 30)
   │                 │           └─ TableScan tpcds.time_dim est. ? rows
   │                 └─ Repartition Exchange est. ? rows
   │                    │     Partition by: {s_store_sk_6}
   │                    └─ Filter est. ? rows
   │                       │     s_store_name_6 = 'ese'
   │                       └─ TableScan tpcds.store est. ? rows
   └─ Broadcast Exchange est. ? rows
      └─ MergingAggregated est. ? rows
         └─ Gather Exchange est. ? rows
            └─ Aggregating est. ? rows
               │     Group by: {}
               └─ Inner Join est. ? rows
                  │     Condition: ss_store_sk_7 == s_store_sk_7
                  ├─ Repartition Exchange est. ? rows
                  │  │     Partition by: {ss_store_sk_7}
                  │  └─ Inner Join est. ? rows
                  │     │     Condition: ss_sold_time_sk_7 == t_time_sk_7
                  │     ├─ Repartition Exchange est. ? rows
                  │     │  │     Partition by: {ss_sold_time_sk_7}
                  │     │  └─ Inner Join est. ? rows
                  │     │     │     Condition: ss_hdemo_sk_7 == hd_demo_sk_7
                  │     │     ├─ Repartition Exchange est. ? rows
                  │     │     │  │     Partition by: {ss_hdemo_sk_7}
                  │     │     │  └─ TableScan tpcds.store_sales est. ? rows
                  │     │     └─ Repartition Exchange est. ? rows
                  │     │        │     Partition by: {hd_demo_sk_7}
                  │     │        └─ Filter est. ? rows
                  │     │           │     ((hd_vehicle_count_7 <= cast(2, 'UInt16')) AND (hd_dep_count_7 = 0)) OR ((hd_vehicle_count_7 <= cast(6, 'UInt16')) AND (hd_dep_count_7 = 4)) OR ((hd_vehicle_count_7 <= cast(4, 'UInt16')) AND (hd_dep_count_7 = 2))
                  │     │           └─ TableScan tpcds.household_demographics est. ? rows
                  │     └─ Repartition Exchange est. ? rows
                  │        │     Partition by: {t_time_sk_7}
                  │        └─ Filter est. ? rows
                  │           │     (t_minute_7 < 30) AND (t_hour_7 = 12)
                  │           └─ TableScan tpcds.time_dim est. ? rows
                  └─ Repartition Exchange est. ? rows
                     │     Partition by: {s_store_sk_7}
                     └─ Filter est. ? rows
                        │     s_store_name_7 = 'ese'
                        └─ TableScan tpcds.store est. ? rows
