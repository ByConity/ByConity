Projection est. 100 rows
└─ Limit est. 100 rows
   └─ MergingSorted est. ? rows
      └─ MergeSorting est. ? rows
         └─ PartialSorting est. ? rows
            └─ Gather Exchange est. ? rows
               └─ MergeSorting est. ? rows
                  └─ PartialSorting est. ? rows
                     └─ Inner Join est. ? rows
                        │     Condition: ss_item_sk_2 == i_item_sk_1
                        ├─ Repartition Exchange est. ? rows
                        │  │     Partition by: {ss_item_sk_2}
                        │  └─ Inner Join est. ? rows
                        │     │     Condition: ss_item_sk == i_item_sk
                        │     ├─ Repartition Exchange est. ? rows
                        │     │  │     Partition by: {ss_item_sk}
                        │     │  └─ Inner Join est. ? rows
                        │     │     │     Condition: expr#rank() OVER (ORDER BY rank_col ASC) == expr#rank() OVER (ORDER BY rank_col DESC)
                        │     │     ├─ Repartition Exchange est. ? rows
                        │     │     │  │     Partition by: {expr#rank() OVER (ORDER BY rank_col ASC)}
                        │     │     │  └─ Filter est. ? rows
                        │     │     │     │     `expr#rank() OVER (ORDER BY rank_col ASC)` < 11
                        │     │     │     └─ Window est. ? rows
                        │     │     │        └─ Gather Exchange est. ? rows
                        │     │     │           └─ Inner Join est. ? rows
                        │     │     │              │     Condition: 
                        │     │     │              │     Filter: `expr#avg(ss_net_profit)` > (0.9 * `expr#avg(ss_net_profit)_1`)
                        │     │     │              ├─ MergingAggregated est. ? rows
                        │     │     │              │  └─ Repartition Exchange est. ? rows
                        │     │     │              │     │     Partition by: {ss_item_sk}
                        │     │     │              │     └─ Aggregating est. ? rows
                        │     │     │              │        │     Group by: {ss_item_sk}
                        │     │     │              │        └─ Filter est. ? rows
                        │     │     │              │           │     ss_store_sk = 4
                        │     │     │              │           └─ TableScan tpcds.store_sales est. ? rows
                        │     │     │              └─ Broadcast Exchange est. ? rows
                        │     │     │                 └─ EnforceSingleRow est. ? rows
                        │     │     │                    └─ Union est. ? rows
                        │     │     │                       └─ Gather Exchange est. ? rows
                        │     │     │                          └─ Projection est. ? rows
                        │     │     │                             └─ MergingAggregated est. ? rows
                        │     │     │                                └─ Repartition Exchange est. ? rows
                        │     │     │                                   │     Partition by: {ss_store_sk_1}
                        │     │     │                                   └─ Aggregating est. ? rows
                        │     │     │                                      │     Group by: {ss_store_sk_1}
                        │     │     │                                      └─ Filter est. ? rows
                        │     │     │                                         │     (ss_store_sk_1 = 4) AND isNull(ss_addr_sk_1)
                        │     │     │                                         └─ TableScan tpcds.store_sales est. ? rows
                        │     │     └─ Repartition Exchange est. ? rows
                        │     │        │     Partition by: {expr#rank() OVER (ORDER BY rank_col DESC)}
                        │     │        └─ Filter est. ? rows
                        │     │           │     `expr#rank() OVER (ORDER BY rank_col DESC)` < 11
                        │     │           └─ Window est. ? rows
                        │     │              └─ Gather Exchange est. ? rows
                        │     │                 └─ Inner Join est. ? rows
                        │     │                    │     Condition: 
                        │     │                    │     Filter: `expr#avg(ss_net_profit)_2` > (0.9 * `expr#avg(ss_net_profit)_3`)
                        │     │                    ├─ MergingAggregated est. ? rows
                        │     │                    │  └─ Repartition Exchange est. ? rows
                        │     │                    │     │     Partition by: {ss_item_sk_2}
                        │     │                    │     └─ Aggregating est. ? rows
                        │     │                    │        │     Group by: {ss_item_sk_2}
                        │     │                    │        └─ Filter est. ? rows
                        │     │                    │           │     ss_store_sk_2 = 4
                        │     │                    │           └─ TableScan tpcds.store_sales est. ? rows
                        │     │                    └─ Broadcast Exchange est. ? rows
                        │     │                       └─ EnforceSingleRow est. ? rows
                        │     │                          └─ Union est. ? rows
                        │     │                             └─ Gather Exchange est. ? rows
                        │     │                                └─ Projection est. ? rows
                        │     │                                   └─ MergingAggregated est. ? rows
                        │     │                                      └─ Repartition Exchange est. ? rows
                        │     │                                         │     Partition by: {ss_store_sk_3}
                        │     │                                         └─ Aggregating est. ? rows
                        │     │                                            │     Group by: {ss_store_sk_3}
                        │     │                                            └─ Filter est. ? rows
                        │     │                                               │     (ss_store_sk_3 = 4) AND isNull(ss_addr_sk_3)
                        │     │                                               └─ TableScan tpcds.store_sales est. ? rows
                        │     └─ Repartition Exchange est. ? rows
                        │        │     Partition by: {i_item_sk}
                        │        └─ TableScan tpcds.item est. ? rows
                        └─ Repartition Exchange est. ? rows
                           │     Partition by: {i_item_sk_1}
                           └─ TableScan tpcds.item est. ? rows
