Projection est. ? rows
└─ MergingAggregated est. ? rows
   └─ Gather Exchange est. ? rows
      └─ Aggregating est. ? rows
         │     Group by: {}
         └─ Inner Join est. ? rows
            │     Condition: ss_sold_date_sk == d_date_sk
            ├─ Repartition Exchange est. ? rows
            │  │     Partition by: {ss_sold_date_sk}
            │  └─ Inner Join est. ? rows
            │     │     Condition: ss_addr_sk == ca_address_sk
            │     │     Filter: ((ca_state IN ('VA', 'CA', 'MS')) AND (ss_net_profit <= 25000) AND (ss_net_profit >= 50)) OR ((ss_net_profit >= 0) AND (ss_net_profit <= 2000) AND (ca_state IN ('CO', 'OH', 'TX'))) OR ((ss_net_profit <= 3000) AND (ca_state IN ('OR', 'MN', 'KY')) AND (ss_net_profit >= 150))
            │     ├─ Repartition Exchange est. ? rows
            │     │  │     Partition by: {ss_addr_sk}
            │     │  └─ Inner Join est. ? rows
            │     │     │     Condition: ss_cdemo_sk == cd_demo_sk
            │     │     │     Filter: ((cd_education_status = 'College') AND (cd_marital_status = 'S') AND (ss_sales_price <= 200.) AND (ss_sales_price >= 150.)) OR ((ss_sales_price <= 150.) AND (cd_education_status = '4 yr Degree') AND (cd_marital_status = 'M') AND (ss_sales_price >= 100.)) OR ((cd_marital_status = 'D') AND (ss_sales_price <= 100.) AND (ss_sales_price >= 50.) AND (cd_education_status = '2 yr Degree'))
            │     │     ├─ Repartition Exchange est. ? rows
            │     │     │  │     Partition by: {ss_cdemo_sk}
            │     │     │  └─ Inner Join est. ? rows
            │     │     │     │     Condition: ss_store_sk == s_store_sk
            │     │     │     ├─ Repartition Exchange est. ? rows
            │     │     │     │  │     Partition by: {ss_store_sk}
            │     │     │     │  └─ TableScan tpcds.store_sales est. ? rows
            │     │     │     └─ Repartition Exchange est. ? rows
            │     │     │        │     Partition by: {s_store_sk}
            │     │     │        └─ TableScan tpcds.store est. ? rows
            │     │     └─ Repartition Exchange est. ? rows
            │     │        │     Partition by: {cd_demo_sk}
            │     │        └─ TableScan tpcds.customer_demographics est. ? rows
            │     └─ Repartition Exchange est. ? rows
            │        │     Partition by: {ca_address_sk}
            │        └─ Filter est. ? rows
            │           │     ca_country = 'United States'
            │           └─ TableScan tpcds.customer_address est. ? rows
            └─ Repartition Exchange est. ? rows
               │     Partition by: {d_date_sk}
               └─ Filter est. ? rows
                  │     d_year = 2000
                  └─ TableScan tpcds.date_dim est. ? rows
