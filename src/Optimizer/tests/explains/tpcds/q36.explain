Projection est. 100 rows
└─ Limit est. 100 rows
   └─ MergingSorted est. 1000 rows
      └─ MergeSorting est. 1000 rows
         └─ PartialSorting est. 1000 rows
            └─ Gather Exchange est. 1000 rows
               └─ MergeSorting est. 1000 rows
                  └─ PartialSorting est. 1000 rows
                     └─ Projection est. 1000 rows
                        └─ Window est. 1000 rows
                           └─ Repartition Exchange est. 1000 rows
                              │     Partition by: {expr#multiIf(equals(grouping(i_class), 0), i_category, NULL), expr#plus(grouping(i_category), grouping(i_class))}
                              └─ Projection est. 1000 rows
                                 └─ Aggregating est. 1000 rows
                                    │     Group by: {i_category, i_class}
                                    └─ Gather Exchange est. 567906871 rows
                                       └─ Inner Join est. 567906871 rows
                                          │     Condition: ss_store_sk == s_store_sk
                                          ├─ Inner Join est. 567906871 rows
                                          │  │     Condition: ss_item_sk == i_item_sk
                                          │  ├─ Repartition Exchange est. 567906871 rows
                                          │  │  │     Partition by: {ss_item_sk}
                                          │  │  └─ Inner Join est. 567906871 rows
                                          │  │     │     Condition: ss_sold_date_sk == d_date_sk
                                          │  │     ├─ Local Exchange est. 2879987999 rows
                                          │  │     │  └─ Filter est. 2879987999 rows
                                          │  │     │     │     Dynamic Filters: {ss_sold_date_sk:range}
                                          │  │     │     └─ TableScan tpcds.store_sales est. 2879987999 rows
                                          │  │     └─ Projection est. 365 rows
                                          │  │        │     Dynamic Filters Builder: {d_date_sk}
                                          │  │        └─ Broadcast Exchange est. 365 rows
                                          │  │           └─ Filter est. 365 rows
                                          │  │              │     d_year = 2001
                                          │  │              └─ TableScan tpcds.date_dim est. 73049 rows
                                          │  └─ Repartition Exchange est. 300000 rows
                                          │     │     Partition by: {i_item_sk}
                                          │     └─ TableScan tpcds.item est. 300000 rows
                                          └─ Broadcast Exchange est. 1002 rows
                                             └─ Filter est. 1002 rows
                                                │     s_state IN ('TN', 'TN', 'TN', 'TN', 'TN', 'TN', 'TN', 'TN')
                                                └─ TableScan tpcds.store est. 1002 rows
note: Dynamic Filter is applied for 1 times.
