Projection est. 100 rows
└─ Limit est. 100 rows
   └─ MergingSorted est. 77976000 rows
      └─ MergeSorting est. 77976000 rows
         └─ PartialSorting est. 77976000 rows
            └─ Gather Exchange est. 77976000 rows
               └─ MergeSorting est. 77976000 rows
                  └─ PartialSorting est. 77976000 rows
                     └─ Projection est. 77976000 rows
                        └─ Filter est. 77976000 rows
                           │     multiIf(`expr#avg(sum(ss_sales_price)) OVER (PARTITION BY i_category, i_brand, s_store_name, s_company_name)` != 0, abs(`expr#sum(ss_sales_price)` - `expr#avg(sum(ss_sales_price)) OVER (PARTITION BY i_category, i_brand, s_store_name, s_company_name)`) / `expr#avg(sum(ss_sales_price)) OVER (PARTITION BY i_category, i_brand, s_store_name, s_company_name)`, NULL) > 0.1
                           └─ Window est. 86640000 rows
                              └─ Repartition Exchange est. 86640000 rows
                                 │     Partition by: {i_brand, i_category, s_company_name, s_store_name}
                                 └─ MergingAggregated est. 86640000 rows
                                    └─ Repartition Exchange est. 86640000 rows
                                       │     Partition by: {d_moy, i_brand, i_category, i_class, s_company_name, s_store_name}
                                       └─ Aggregating est. 86640000 rows
                                          │     Group by: {d_moy, i_brand, i_category, i_class, s_company_name, s_store_name}
                                          └─ Inner Join est. 2879987999 rows
                                             │     Condition: ss_store_sk == s_store_sk
                                             ├─ Inner Join est. 2879987999 rows
                                             │  │     Condition: ss_sold_date_sk == d_date_sk
                                             │  ├─ Inner Join est. 2879987999 rows
                                             │  │  │     Condition: ss_item_sk == i_item_sk
                                             │  │  ├─ Repartition Exchange est. 2879987999 rows
                                             │  │  │  │     Partition by: {ss_item_sk}
                                             │  │  │  └─ TableScan tpcds.store_sales est. 2879987999 rows
                                             │  │  └─ Repartition Exchange est. 300000 rows
                                             │  │     │     Partition by: {i_item_sk}
                                             │  │     └─ Filter est. 300000 rows
                                             │  │        │     ((i_class IN ('shirts', 'birdal', 'dresses')) OR (i_class IN ('computers', 'stereo', 'football'))) AND ((i_category IN ('Men', 'Jewelry', 'Women')) OR (i_category IN ('Books', 'Electronics', 'Sports'))) AND ((i_category IN ('Men', 'Jewelry', 'Women')) OR (i_class IN ('computers', 'stereo', 'football'))) AND ((i_class IN ('shirts', 'birdal', 'dresses')) OR (i_category IN ('Books', 'Electronics', 'Sports')))
                                             │  │        └─ TableScan tpcds.item est. 300000 rows
                                             │  └─ Broadcast Exchange est. 73049 rows
                                             │     └─ Filter est. 73049 rows
                                             │        │     d_year IN (1999)
                                             │        └─ TableScan tpcds.date_dim est. 73049 rows
                                             └─ Broadcast Exchange est. 1002 rows
                                                └─ TableScan tpcds.store est. 1002 rows
