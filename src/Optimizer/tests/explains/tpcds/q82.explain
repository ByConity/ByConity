Projection est. 100 rows
└─ Limit est. 100 rows
   └─ MergingSorted est. 11904400512 rows
      └─ MergeSorting est. 11904400512 rows
         └─ PartialSorting est. 11904400512 rows
            └─ Gather Exchange est. 11904400512 rows
               └─ MergeSorting est. 11904400512 rows
                  └─ PartialSorting est. 11904400512 rows
                     └─ MergingAggregated est. 11904400512 rows
                        └─ Repartition Exchange est. 11904400512 rows
                           │     Partition by: {expr#CAST(i_current_price, 'Float64'), i_item_desc, i_item_id}
                           └─ Aggregating est. 11904400512 rows
                              │     Group by: {expr#CAST(i_current_price, 'Float64'), i_item_desc, i_item_id}
                              └─ Projection est. 146342768589 rows
                                 └─ Inner Join est. 146342768589 rows
                                    │     Condition: ss_item_sk == i_item_sk
                                    ├─ Local Exchange est. 2879987999 rows
                                    │  └─ Filter est. 2879987999 rows
                                    │     │     Dynamic Filters: {ss_item_sk:range}
                                    │     └─ TableScan tpcds.store_sales est. 2879987999 rows
                                    └─ Broadcast Exchange est. 15210915 rows
                                       └─ Inner Join est. 15210915 rows
                                          │     Condition: inv_date_sk == d_date_sk
                                          ├─ Inner Join est. 26365587 rows
                                          │  │     Condition: inv_item_sk == i_item_sk
                                          │  ├─ Local Exchange est. 312895332 rows
                                          │  │  └─ Filter est. 312895332 rows
                                          │  │     │     (inv_quantity_on_hand <= 500) AND (inv_quantity_on_hand >= 100)
                                          │  │     │     Dynamic Filters: {inv_date_sk:bloomfilter,inv_date_sk:range,inv_item_sk:bloomfilter,inv_item_sk:range}
                                          │  │     └─ TableScan tpcds.inventory est. 783000000 rows
                                          │  └─ Broadcast Exchange est. 10102 rows
                                          │     └─ Projection est. 10102 rows
                                          │        │     Dynamic Filters Builder: {i_item_sk}
                                          │        └─ Filter est. 10102 rows
                                          │           │     (i_current_price <= 92) AND (i_current_price >= 62) AND (i_manufact_id IN (129, 270, 821, 423))
                                          │           └─ TableScan tpcds.item est. 300000 rows
                                          └─ Broadcast Exchange est. 60 rows
                                             └─ Projection est. 60 rows
                                                │     Dynamic Filters Builder: {d_date_sk}
                                                └─ Filter est. 60 rows
                                                   │     (d_date >= cast(11102, 'Date')) AND (d_date <= cast(11162, 'Date'))
                                                   └─ TableScan tpcds.date_dim est. 73049 rows
note: Dynamic Filter is applied for 5 times.
