Projection est. 67377010 rows
└─ MergingSorted est. 67377010 rows
   └─ MergeSorting est. 67377010 rows
      └─ PartialSorting est. 67377010 rows
         └─ Gather Exchange est. 67377010 rows
            └─ MergeSorting est. 67377010 rows
               └─ PartialSorting est. 67377010 rows
                  └─ Inner Join est. 67377010 rows
                     │     Condition: ss_customer_sk == c_customer_sk
                     ├─ Repartition Exchange est. 67377010 rows
                     │  │     Partition by: {ss_customer_sk}
                     │  └─ Filter est. 67377010 rows
                     │     │     (`expr#count()` <= 20) AND (`expr#count()` >= 15)
                     │     └─ MergingAggregated est. 74863345 rows
                     │        └─ Repartition Exchange est. 74863345 rows
                     │           │     Partition by: {ss_customer_sk, ss_ticket_number}
                     │           └─ Aggregating est. 74863345 rows
                     │              │     Group by: {ss_customer_sk, ss_ticket_number}
                     │              └─ Inner Join est. 74863345 rows
                     │                 │     Condition: ss_store_sk == s_store_sk
                     │                 ├─ Inner Join est. 74863345 rows
                     │                 │  │     Condition: ss_hdemo_sk == hd_demo_sk
                     │                 │  ├─ Inner Join est. 368750489 rows
                     │                 │  │  │     Condition: ss_sold_date_sk == d_date_sk
                     │                 │  │  ├─ Local Exchange est. 2879987999 rows
                     │                 │  │  │  └─ Filter est. 2879987999 rows
                     │                 │  │  │     │     Dynamic Filters: {ss_hdemo_sk:bloomfilter,ss_hdemo_sk:range,ss_sold_date_sk:bloomfilter,ss_sold_date_sk:range}
                     │                 │  │  │     └─ TableScan tpcds.store_sales est. 2879987999 rows
                     │                 │  │  └─ Broadcast Exchange est. 237 rows
                     │                 │  │     └─ Projection est. 237 rows
                     │                 │  │        │     Dynamic Filters Builder: {d_date_sk}
                     │                 │  │        └─ Filter est. 237 rows
                     │                 │  │           │     (d_year IN (1999, 2000, 2001)) AND (((d_dom >= 25) AND (d_dom <= 28)) OR ((d_dom <= 3) AND (d_dom >= 1)))
                     │                 │  │           └─ TableScan tpcds.date_dim est. 73049 rows
                     │                 │  └─ Broadcast Exchange est. 1466 rows
                     │                 │     └─ Projection est. 1466 rows
                     │                 │        │     Dynamic Filters Builder: {hd_demo_sk}
                     │                 │        └─ Filter est. 1466 rows
                     │                 │           │     (multiIf(hd_vehicle_count > 0, hd_dep_count / hd_vehicle_count, NULL) > 1.2) AND (hd_vehicle_count > 0) AND ((hd_buy_potential = 'Unknown') OR (hd_buy_potential = '>10000'))
                     │                 │           └─ TableScan tpcds.household_demographics est. 7200 rows
                     │                 └─ Broadcast Exchange est. 1002 rows
                     │                    └─ Filter est. 1002 rows
                     │                       │     s_county IN ('Williamson County', 'Williamson County', 'Williamson County', 'Williamson County', 'Williamson County', 'Williamson County', 'Williamson County', 'Williamson County')
                     │                       └─ TableScan tpcds.store est. 1002 rows
                     └─ Repartition Exchange est. 12000000 rows
                        │     Partition by: {c_customer_sk}
                        └─ TableScan tpcds.customer est. 12000000 rows
note: Dynamic Filter is applied for 4 times.
