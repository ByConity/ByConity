----test reorder adjacent windows
Projection Est. ? rows
│     Expressions: [AVG(age), RANK(), count(major), id, major]
└─ Sorting Est. ? rows
   │     Order by: {major asc, id asc}
   └─ Gather Exchange Est. ? rows
      └─ Sorting Est. ? rows
         │     Order by: {major asc, id asc}
         └─ Window Est. ? rows
            └─ Local Exchange Est. ? rows
               └─ Window Est. ? rows
                  └─ Local Exchange Est. ? rows
                     └─ Window Est. ? rows
                        └─ Repartition Exchange Est. ? rows
                           │     Partition by: {major}
                           └─ TableScan test.students Est. ? rows
                                    Outputs: [age, id, major]
----test no swap to adjacent windows
Projection Est. ? rows
│     Expressions: [AVG(age), RANK(), count(major), id, major]
└─ Sorting Est. ? rows
   │     Order by: {major asc, id asc}
   └─ Gather Exchange Est. ? rows
      └─ Sorting Est. ? rows
         │     Order by: {major asc, id asc}
         └─ Window Est. ? rows
            └─ Local Exchange Est. ? rows
               └─ Window Est. ? rows
                  └─ Local Exchange Est. ? rows
                     └─ Window Est. ? rows
                        └─ Repartition Exchange Est. ? rows
                           │     Partition by: {major}
                           └─ TableScan test.students Est. ? rows
                                    Outputs: [age, id, major]
